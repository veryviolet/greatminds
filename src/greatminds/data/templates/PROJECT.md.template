# PROJECT.md

Project-specific narrative context. Read by every agent at tick
start. Keep short and high-signal.

## System variables

Variables that greatminds reads out of the box. Concrete values live
in `.greatminds/PROJECT.env` (gitignored). The list below is
generated from the packaged greatminds schema at setup time.

{{SYSTEM_VARS_DOCS}}

## Custom variables

Add any `KEY=value` to `.greatminds/PROJECT.env` and reference via
`${name}` in this file, in skill bodies, MCP server configs, or any
subprocess. `greatminds start-agent` sources `PROJECT.env` before
launching each agent, so the values appear in the agent's
environment.

## Project context

Free-form prose: project name, root path, stack overview, deploy
recipe, etc. Edit as needed.

Write prose directly and reference env vars via `${name}` shell form.
