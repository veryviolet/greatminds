# coordination/PROJECT.env — runtime KEY=value variables.
# Gitignored. Loaded into the environment of every agent at
# start_agent launch (accessible to skill Bash blocks, MCP servers,
# and any subprocess).
#
# greatminds setup auto-generates the system_vars section below from
# schema.project_env.system_vars. Add your own KEY=value entries
# anywhere in this file — reference them from PROJECT.md or skills
# via ${name} shell-form.

{{SYSTEM_VARS_ENTRIES}}
